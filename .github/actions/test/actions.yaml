name: 'test'
description: 'test'
inputs:
  RELEASE_VERSION:
    description: "release version"
    required: true
runs:
  using: 'composite'
  steps:
    - name: print release version
      run: echo ${{ inputs.RELEASE_VERSION }}