name: first-workflow
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: displaying hello
        run: echo "release_tag=0.0.1" >> $GITHUB_ENV
      - name: printing release tag
        run: echo "${release_tag}"
